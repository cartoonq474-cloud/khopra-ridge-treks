import ClientPage from './ClientPage';

export const metadata = {
  alternates: {
    canonical: '/planning/khayer-lake-altitude-difficulty',
  },
};

export default function Page(props) {
  return <ClientPage {...props} />;
}
